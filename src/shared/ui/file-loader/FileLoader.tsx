import styles from './FileLoader.module.scss';

const FileLoader = () => {
    return (
        <span className={styles.loader}>
            <span />
            <span />
            <span />
        </span>
    );
};

export default FileLoader;